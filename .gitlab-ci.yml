# .gitlab-ci.yml for OpenTPS

stages:
  - setup
  - test_core
  - test_gui
  - unittest_core
  - build
  - docs

variables:
  IMAGE_NAME: $CI_REGISTRY_IMAGE
  IMAGE_TAG: latest

# ------------------------
# Docker build and push
# ------------------------
build:
  stage: build
  script:
    - cd $CI_PROJECT_DIR
    - poetry install --no-root
    - docker build -t $IMAGE_NAME:$IMAGE_TAG .
    - docker push $IMAGE_NAME:$IMAGE_TAG
  rules:
    - changes:
        - Dockerfile
        - opentps_core/opentps/core/pyproject.toml
        - opentps_core/opentps/core/poetry.lock
        - opentps_gui/opentps/gui/poetry.lock
        - opentps_gui/opentps/gui/pyproject.toml
        - pyproject.toml
        - poetry.lock

default:
  image: $IMAGE_NAME:$IMAGE_TAG
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY

# ------------------------
# Core testing
# ------------------------
test_core:
  stage: test_core
  script:
    - cd $CI_PROJECT_DIR
    - python CI/tests.py

test_gui:
  stage: test_gui
  script:
    - cd $CI_PROJECT_DIR
    - export QT_QPA_PLATFORM=offscreen
    - python -m unittest discover opentps_gui/opentps/gui

unittest_core:
  stage: unittest_core
  script:
    - cd $CI_PROJECT_DIR/opentps_core
    - touch opentps/__init__.py __init__.py
    - python -m unittest discover -s ./opentps -p '[!example]*.py' -v


# ------------------------
# Docs
# ------------------------
docs:
  stage: docs
  script:
    - pip install sphinx sphinx-rtd-theme
    - cd docs
    - make html
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
